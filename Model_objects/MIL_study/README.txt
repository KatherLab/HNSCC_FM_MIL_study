Model weights and STAMP-1.0.1 compatible model objects (.pkl) for deployment are stored here for all 3 endpoints and models.
STAMP 1.0.1 uses these models through the config.yaml file with the deploy flag
